# 12 Snake Case

This example shows how the mapper can match `snake_case` database columns to Go struct fields.

For example, the `created_at` column is mapped to the `CreatedAt` field without requiring a `db` tag.